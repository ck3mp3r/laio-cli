---
name: rmux-test

path: .

windows:
  - name: code
    layout: main-horizontal
    panes:
      - type: horizontal
        commands:
          - $EDITOR

  - name: misc
    layout: even-horizontal
    panes:
      - type: horizontal
        path: . # a relative path to root
      - type: horizontal
        path: . # a relative path to root
