---
name: laio

path: .

env:
  foo: bar

windows:
  - name: code
    panes:
      - commands:
          - $EDITOR

  - name: misc
    panes:
      - flex: 1
      - flex: 1
        flex_direction: column
        panes:
          - flex: 1
            path: docs
            commands:
              - zola serve
          - flex: 4
