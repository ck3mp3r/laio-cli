---
name: laio-test

path: .

commands:
  - echo "start"
  - for n in `ls`; do echo $n; sleep 1; done;
  # - foo
  # - sleep 10
  - echo "done"

shutdown:
  - echo "Bye bye"
  - sleep 10

env:
  foo: bar

windows:
  - name: code
    panes:
      - commands:
          - $EDITOR

  - name: misc
    flex_direction: column
    panes:
      - flex: 1
        path: . # a relative path to root
        flex_direction: row
        panes:
          - flex: 1
            flex_direction: column
            panes:
              - flex: 1
                path: ./src
              - flex: 1
                path: src/laio
          - flex: 5
      - flex: 1
        path: . # a relative path to root
